Metadata-Version: 2.4
Name: z-image-ui
Version: 0.1.0
Summary: Add your description here
Requires-Python: >=3.12
Description-Content-Type: text/markdown
Requires-Dist: anthropic>=0.28.0
Requires-Dist: aiofiles>=23.2.0
Requires-Dist: gradio>=6.0.1
Requires-Dist: itchat-uos>=1.4.1
Requires-Dist: pillow>=12.0.0
Requires-Dist: pydantic>=2.5.0
Requires-Dist: python-dotenv>=1.2.1
Requires-Dist: python-telegram-bot>=22.5
Requires-Dist: python-multipart>=0.0.6
Requires-Dist: requests>=2.32.5

# Personal Assistant Bot

基于插件架构的Telegram个人助手机器人，可以轻松集成多种工具和服务。

## 特性

- 🔌 **插件系统**: 模块化架构，轻松添加新功能
- 🎨 **AI图像生成**: 通过Z-Image-Turbo模型生成图像
- ⚙️ **配置管理**: 支持配置文件和环境变量
- 🚀 **易于扩展**: 详细的插件开发指南

## 项目结构

```
z-image-ui/
├── bot/                    # 核心机器人逻辑
│   ├── base_plugin.py     # 插件基类
│   ├── core.py            # 主机器人
│   └── plugin_manager.py  # 插件管理器
├── plugins/               # 插件目录
│   └── image_generation/  # 图像生成插件
├── utils/                 # 共享工具
│   ├── config.py         # 配置管理
│   └── api_client.py     # API客户端
├── config.ini            # 配置文件
├── telegram_bot.py       # 入口点
├── run.sh                # 启动脚本
├── PLUGIN_GUIDE.md       # 插件开发指南
└── README.md             # 本文档
```

## 快速开始

### 1. 安装依赖

```bash
# 使用uv安装依赖
./run.sh install

# 或手动安装
uv sync
```

### 2. 配置

编辑 `config.ini`:

```ini
[modelscope]
api_key = your_modelscope_api_key

[telegram]
bot_token = your_telegram_bot_token
```

或使用环境变量:

```bash
export MODELSCOPE_API_KEY="your_api_key"
export TELEGRAM_BOT_TOKEN="your_bot_token"
```

### 3. 运行机器人

```bash
./run.sh bot
```

## 使用方法

### Telegram Bot命令

- `/start` - 开始使用机器人
- `/help` - 查看所有可用命令
- `/img <提示词>` - 生成图像（快捷方式）
- 直接发送文本 - 作为图像生成提示词

### 示例

```
/img 一只金色的猫在花园里玩耍
```

或直接发送:
```
一座未来城市，有飞行汽车
```

## 添加新插件

查看 `PLUGIN_GUIDE.md` 获取详细的插件开发指南。

### 快速示例

1. 创建插件目录:
```bash
mkdir -p plugins/your_plugin
touch plugins/your_plugin/__init__.py
touch plugins/your_plugin/plugin.py
```

2. 实现插件类（继承 `BasePlugin`）

3. 重启机器人 - 插件会自动加载！

## 当前插件

### 图像生成 (Image Generation)
- 使用Z-Image-Turbo模型生成AI图像
- 支持中英文提示词
- 命令: `/img`

## 配置说明

### ModelScope配置
```ini
[modelscope]
base_url = https://api-inference.modelscope.cn/
model_id = Tongyi-MAI/Z-Image-Turbo
api_key = your_api_key
```

### Telegram配置
```ini
[telegram]
bot_token = your_bot_token
```

## 开发

### 依赖管理
本项目使用 `uv` 进行依赖管理:

```bash
# 添加新依赖
uv add package_name

# 同步依赖
uv sync

# 运行脚本
uv run script.py
```

### 项目命令

```bash
./run.sh          # 启动Telegram Bot
./run.sh install  # 安装依赖
./run.sh help     # 查看帮助
```

## 故障排除

### Bot无法启动
1. 检查 `config.ini` 中的配置
2. 确认环境变量已设置
3. 查看终端错误信息

### 图像生成失败
1. 检查ModelScope API密钥是否正确
2. 确认网络连接正常
3. 查看日志中的错误信息

## 许可证

本项目使用的Z-Image-Turbo模型遵循 Apache License 2.0。

## 贡献

欢迎贡献新的插件和功能！请参考 `PLUGIN_GUIDE.md` 了解如何开发插件。
